# minio_bucket.yaml
type: s3
config:
  bucket: thanos # bucket 名称
  access_key: admin
  secret_key: admin123
  insecure: true
  signature_version2: false

  # endpoint: minio.dev-nightwatch.svc.cluster.local:9000 # minio 访问地址
  # access_key: minio
  # secret_key: minio123
  # insecure: true
  # signature_version2: false

  